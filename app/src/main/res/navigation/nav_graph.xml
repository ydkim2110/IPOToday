<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools" android:id="@+id/nav_graph"
            app:startDestination="@id/homeFragment">

    <fragment
            android:id="@+id/homeFragment"
            android:name="com.ipotoday.ipotoday.ui.home.HomeFragment"
            android:label="Home"
            tools:layout="@layout/fragment_home">
    </fragment>

    <fragment
            android:id="@+id/calendarFragment"
            android:name="com.ipotoday.ipotoday.ui.calendar.CalendarFragment"
            android:label="Calendar"
            tools:layout="@layout/fragment_calendar">
    </fragment>

    <fragment
            android:id="@+id/bookmarkFragment"
            android:name="com.ipotoday.ipotoday.ui.bookmark.BookmarkFragment"
            android:label="BookMark"
            tools:layout="@layout/fragment_bookmark">
    </fragment>

    <fragment
            android:id="@+id/settingsFragment"
            android:name="com.ipotoday.ipotoday.ui.settings.SettingsFragment"
            android:label="Settings"
            tools:layout="@layout/fragment_settings">
    </fragment>

</navigation>